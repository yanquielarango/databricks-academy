# This is a Declarative Automation Bundle definition for Lab_3.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.

bundle:
  name: Lab_3
  uuid: 8c825461-2fd6-4b63-b457-77e50116a679

include:
  - resources/*.yml

variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:

  dev:
    mode: development
    default: true

    workspace:
      host: https://adb-7405604503619901.1.azuredatabricks.net
      root_path: /Workspace/Users/yanquiel@softserve.academy/.bundle/${bundle.name}/${bundle.target}

    variables:
      catalog: dbr_dev
      schema: ${workspace.current_user.short_name}

  prod:
    mode: production

    workspace:
      host: https://adb-7405604503619901.1.azuredatabricks.net
      root_path: /Workspace/Users/yanquiel@softserve.academy/.bundle/${bundle.name}/${bundle.target}

    variables:
      catalog: dbr_dev
      schema: prod

    permissions:
      - user_name: yanquiel@softserve.academy
        level: CAN_MANAGE
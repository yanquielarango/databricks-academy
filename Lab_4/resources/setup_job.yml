resources:
  jobs:
    setup_job:

      name: ${bundle.name}-setup

      tasks:

        - task_key: create_objects

          notebook_task:
            notebook_path: ../notebooks/setup/create_objects.ipynb
            base_parameters:
              environment: ${bundle.target}
              cluster_id: ${var.cluster_id}
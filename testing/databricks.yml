# This is a Databricks asset bundle definition for testing.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: testing
  uuid: d470a1a6-1576-4696-a866-e5fff3e7d8cb

include:
  - resources/*.yml

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://adb-7405610741739204.4.azuredatabricks.net
    variables:
      catalog: dbr_dev
      schema: ${workspace.current_user.short_name}
  prod:
    mode: production
    workspace:
      host: https://adb-7405610741739204.4.azuredatabricks.net
      # We explicitly deploy to /Workspace/Users/yanquiel.arango@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/yanquiel.arango@gmail.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: dbr_dev
      schema: prod
    permissions:
      - user_name: yanquiel.arango@gmail.com
        level: CAN_MANAGE
